# Server Configuration
PORT=4000
NODE_ENV=development
LOG_LEVEL=info

# A2A Protocol Configuration
# Public base URL for the agent (optional, defaults to http://localhost:4000)
# Used in agent card for external access. Useful for ngrok, tunneling, or production domains.
A2A_BASE_URL=http://localhost:4000

# Kaiban Platform Configuration
# Kaiban platform tenant identifier (required)
KAIBAN_TENANT=your-tenant
# Authentication token for Kaiban API (required)
KAIBAN_API_TOKEN=your-kaiban-api-token
# Kaiban API base URL (optional, defaults to https://${KAIBAN_TENANT}.kaiban.io/api)
KAIBAN_API_URL=https://your-tenant.kaiban.io/api

# Visit Planner Agent Configuration
# Unique identifier for Visit Planner agent in Kaiban (required)
KAIBAN_AGENT_ID=your-visit-planner-agent-id
# OpenAI API key for GPT-4o-mini model (required for Visit Planner Agent)
OPENAI_API_KEY=your-openai-api-key

# Airport Services Agent Configuration
# Unique identifier for Airport Services agent in Kaiban (optional, falls back to KAIBAN_AGENT_ID)
KAIBAN_AIRPORT_SERVICES_AGENT_ID=your-airport-services-agent-id

# Optional. AWS Bedrock Configuration (for Airport Services Agent)
# AWS region for Bedrock (optional, defaults to us-east-1)
AWS_REGION=us-east-1
# AWS access key ID (required for Airport Services Agent)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS secret access key (required for Airport Services Agent)
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
